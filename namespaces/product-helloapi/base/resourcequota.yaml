apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
  # namespace inject edilmez — Kustomize overlay'leri namespace: field'ını enjekte eder
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"
